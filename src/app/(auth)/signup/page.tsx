import type { Metadata } from 'next';
import { Signup } from '@/components/features/auth';

export const metadata: Metadata = {
    title: 'Sign Up | avom-brice realtime collaborative app',
    icons: {
        icon: '/favicon.ico',
    },
};

export default Signup;
